import { IPaginationProps } from './Pagination.types';

export const paginationTestProps: IPaginationProps = {
    currentPage: 1,
    recordsCount: 12,
    recordsPerPage: 10,
    navigateUrl: '/users'
};
